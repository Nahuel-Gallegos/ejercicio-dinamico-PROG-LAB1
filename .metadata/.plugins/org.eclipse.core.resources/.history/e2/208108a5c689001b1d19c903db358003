/*
 * EJERCICIO 2
Remplazar los ' ? ' al siguiente programa [DESCARGAR], para lograr la siguiente funcionalidad.

- Limpie la pantalla
- Asigne a las variables numero1 y numero2 los valores obtenidos
- Realice la resta de dichas variables
- Muestre el resultado por pantalla
 */

#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	setbuf(stdout,NULL);//no me acuerdo para que era pero sin esto no anda en Wndws.

	int numero1;
	int numero2;
	int resta;

	printf("Ingrese el primer numero: \n");
	scanf("%d", &numero1);
	printf("Ingrese el segundo numero: \n");
	scanf("%d", &numero2);

	resta = numero1 - numero2;

	printf("El resultado de la resta es: %d", resta);

	return EXIT_SUCCESS;
}
